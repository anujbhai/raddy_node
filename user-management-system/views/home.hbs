<h1>Welcome</h1>

<div class="table-responsive">
  <table class="table table-striped table-sm">
    <thead>
      <tr>
        <th scope="col">#</th>
        <th scope="col">First</th>
        <th scope="col">Last</th>
        <th scope="col">Email</th>
        <th scope="col">Phone</th>
        <th scope="col" class="d-flex justify-content-end">Action</th>
      </tr>
    </thead>
    <tbody>
      {{#each rows}}
      <tr>
        <th scope="row">{{this.id}}</th>
        <td>{{this.first_name}}</td>
        <td>{{this.last_name}}</td>
        <td>{{this.email}}</td>
        <td>{{this.phone}}</td>
        <td class="d-flex justify-content-end">
          <div>
            <a href="/" type="button" class="btn btn-sm btn-light"><i class="bi bi-eye"></i></a>
            <a href="/" type="button" class="btn btn-sm btn-light"><i class="bi bi-pencil"></i></a>
            <a href="/" type="button" class="btn btn-sm btn-light"><i class="bi bi-trash"></i></a>
          </div>
        </td>
      </tr>
      {{/each}}
    </tbody>
  </table>
</div>